
export interface DBNames extends UserDBNames {
    groupItemsDB: string;
    groupItemsNameOnlyDB: string;
    currency: string;
}


interface UserDBNames {
    userProfileURL: string;
    userEmail: string;
}